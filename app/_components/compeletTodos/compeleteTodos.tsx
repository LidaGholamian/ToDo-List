"use client";

import { useTodoContext } from "@/app/context";
import React, { useState } from "react";

export const CompletedTasks: React.FC = () => {
  const { state, dispatch } = useTodoContext();

  return <h1>dhfgvbm</h1>;
};
